{
  "name": "folder-git-sync-secure",
  "version": "1.0.0",
  "description": "SSH-only Obsidian desktop plugin for syncing configured vault folders to separate Git repositories.",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.1",
    "obsidian": "^1.8.10",
    "typescript": "^5.8.2"
  }
}
